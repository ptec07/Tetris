# Tetris 프로젝트 실행 지침 (Zig)

이 문서는 Zig 기반 테트리스 구현 시 따라야 할 기준 계획서이다.

## 목표
- 기본 테트리스 게임(10x20 보드) + 배경음악(BGM) 포함 구현
- SDL 기반 렌더링/입력/오디오 파이프라인 기반으로 데스크톱 동작
- 구현 단계를 명확히 분리해 작은 단위로 안정적으로 확장

## 1) 범위 정의
- 보드: 가로 10, 세로 20
- 조각: 7-bag 랜덤 생성
- 기능: 좌우 이동, 회전, 소프트 드롭, 하드 드롭, 라인 클리어, 점수/레벨, 게임오버
- 음향: 배경음악 루프 재생, 게임 상태와 동기화된 일시정지/재개

### 구현 진행
- 2026-02-28: 1단계 (SDL 창 생성, 메인 루프, 입력 처리 뼈대) 구현
- 2026-02-28: 2단계 (보드/조각/물리/라인클리어, 상태/점수 통합) 구현
- 2026-02-28: 3단계 (SDL2_mixer 연동 뼈대, BGM 재생/일시정지/게임오버 처리, DAS/ARR 입력 반복) 진행
- 2026-02-28: 4~6단계 (점수·레벨 정책 정교화, HUD 상태표시 렌더링, BGM 신뢰성 제어) 완료

## 2) 기술 스택
- 렌더/입력: SDL2
- 오디오: SDL2_mixer(또는 동일 품질 대체 구현)
- 빌드: Zig(`zig build`) + C 라이브러리 링크
- 리소스: `assets/` 디렉토리 사용 (`.ogg` 등)

## 3) 파일 구조
- `src/main.zig` : 부트스트랩, SDL/오디오 초기화, 게임 상태 진입
- `src/game.zig` : 상태 머신, 메인 루프 제어
- `src/board.zig` : 보드 배열, 충돌 판정, 라인 제거
- `src/piece.zig` : 조각 데이터, 회전/이동 상태
- `src/input.zig` : 키 입력 매핑 및 DAS/ARR 제어
- `src/audio.zig` : BGM 로드, 루프, 볼륨, pause/resume
- `src/random.zig` : 7-bag 난수/셔플
- `assets/` : 음악 파일 및 필요 리소스

## 4) 구현 우선순위
1. 루프/윈도우: 창 생성, 입력 처리, 렌더링 틀
2. 보드/조각: 스폰, 이동, 충돌, 회전
3. 낙하/고정: 소프트/하드 드롭, 라인 클리어
4. 점수/레벨: 클리어 방식별 점수, 레벨 기반 속도 증가
5. 상태 UI: Menu / Playing / Paused / GameOver
6. BGM 통합: 시작-재생, 일시정지-중단, 끝나면 루프 재생
7. 폴리시 정리: 오디오 중복재생 방지, 오버플로우 처리, 입력 반응성 개선

## 5) 타이밍/루프 원칙
- 렌더링 루프(예: 60 FPS)와 낙하 tick을 분리
- 게임 로직은 고정 간격 tick으로 상태 업데이트
- 입력은 즉시 반응 + 반복 이동은 DAS/ARR로 처리

## 6) 오디오 규칙
- 게임 시작 시 `playMusic(..., loop=true)` 1회 호출
- Pause/Resume/Exit 시 오디오 상태를 게임 상태와 일치
- 동일 프레임/이벤트에서 BGM 이중 시작 금지

7) 완성 체크리스트
- 회전 시 벽/바닥 충돌 검사 정확성
- 라인 클리어 시 배열 압축 정확성
- 최상단 막힘(게임오버) 판정
- 하드드롭/소프트드롭의 점수/중력 타이밍 일치
- 오디오 루프 및 음량 제어 정상 동작
- README에 SDL2/SDL2_mixer 의존성, 실행 방법, 오디오 라이선스 요약 기록

## 7) 변경 기준
- 구현 중 모듈 추가/리팩터링은 위 항목의 순서를 깨지 말 것
- 큰 변경은 기능 단위 커밋 단위로 정리
- 오디오, 입력, 물리 판정은 서로 의존도가 낮게 분리 유지

- 2026-02-28: 6단계 마무리 - 오디오 상태 동기화 정리 + 점수/레벨/라인 점수 포화 오버플로 처리 반영
- 2026-02-28: 컴파일/실행 검증 - `zig build` 및 `zig build-obj`에서 실행 환경의 SDL2/SDL2_mixer 라이브러리 및 헤더 미설치로 실패. 코드 수정으로 파서/문법 오류는 정리했으나 런타임 실행 확인은 SDL 종속성 설치 후 필요.

- 2026-02-28: 의존성 설치(`brew install sdl2 sdl2_mixer`) 후 `zig build` 성공. SDL 이벤트/오디오/오버플로 제약 대응으로 컴파일 에러 정리 완료.
- 2026-02-28: `zig build` 통과. `zig build run`을 실행해 시작 검증 (단, 종료는 수동 Ctrl-C로 중단, 런타임 즉시 크래시/빌드 에러 재현 없음).
- 2026-02-28: README.md 추가(빌드/실행/의존성/입력/오디오/테스트 체크리스트 포함)
- 2026-02-28: assets 안내 문서(`assets/README.txt`) 추가, BGM 파일 미포함 정책 명시
- 2026-02-28: `scripts/smoke.sh` 추가 (5초 시작체크용), `zig build` + smoke 실행으로 런타임 시작 검증 완료
- 2026-02-28: `ffmpeg` 설치 후 테스트용 `assets/tetris.ogg`(3초 sine tone, OGG/Opus) 생성 완료.
- 2026-02-28: `assets/tetris.ogg` 파일 존재 확인 (`zig build run` 실행 시 BGM 로드 경로 유효).
- 2026-02-28: 요청하신 YouTube 트랙(`https://www.youtube.com/watch?v=Br2K_L0cqpk`) 오디오를 추출해 `assets/tetris.ogg`에 교체 반영.
- 2026-02-28: `zig build` 성공 후 `zig build run` 실행 로그상 즉시 오류 없이 시작되는 것 확인. SDL 오디오 초기화/미디어 로딩 경로에서 크래시 없음(실행은 수동 중단).
- 2026-02-28: `SDL2_mixer` 실행 환경에서 Opus 디코더 의존(`Missing opus.framework`) 문제로 OGG(Opus) 재생 실패 가능성 확인. BGM을 WAV(`assets/tetris.wav`)로 변환 후 `music_path`를 WAV로 변경해 재생 안정성 확보.
- 2026-02-28: 라인 클리어 시 즉시 삭제 대신 애니메이션 큐를 두고, 140ms 동안 반짝임 오버레이 후 삭제되도록 `board.zig`(대기 삭제 라인 판별/삭제 API) + `game.zig`(클리어 대기/점수 반영 타이밍/렌더 반짝임) 수정.
